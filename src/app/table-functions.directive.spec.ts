import { TableFunctionsDirective } from './table-functions.directive';

describe('TableFunctionsDirective', () => {
  it('should create an instance', () => {
    const directive = new TableFunctionsDirective();
    expect(directive).toBeTruthy();
  });
});
